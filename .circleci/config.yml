version: 2.1

jobs:
  build:
    docker:
      - image: jcemelanda/programeempython:0.0.6
    steps:
        - checkout
        - run: make publish
        - run: make github

workflows:
  publish-article:
    jobs:
      - build